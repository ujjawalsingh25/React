// The file extension should always follow ".jsx" name to run through VITE.

function Name() {                   // naming Convention should be follow as first letter should start with capital always
    return (                        // return should always in parenthesis neither in curly braces or any brackets.
        <h2> Ujjawal Singh</h2>
    );
}


export default Name;
